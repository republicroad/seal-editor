# seal-demo consumes the PUBLISHED npm packages — workspace linking would
# defeat the whole point of this app (roadmap §2 "seal-demo 打通" is the
# npm-consumption validation surface). Keep resolution registry-based.
link-workspace-packages=false
